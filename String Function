#include <iostream>
#include <vector>
#include <iomanip>
#include <string>

using namespace std;
int main(){
    string name;
    cout << "Enter your first name: ";
    getline(cin, name);
    //cout << name.erase(0, 3);
    //cout << name.find('h');
    //cout << name.insert(3, "@");
    //string nickName = name.substr(0, 5);
    // char n1 = name.at(0);
    //string email = name.append("23@gmail.com");
    //cout << name.length();
    //cout << name.empty();
   //name.clear();
}
