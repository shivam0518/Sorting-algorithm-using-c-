#include <iostream>
using namespace std;
int main(){
      int n, revNo = 0, lastDigit;
      cin >> n;
      while (n > 0){
        lastDigit = n % 10;
        n /= 10;
        revNo = revNo * 10 + lastDigit;
      }
      cout << revNo;
}
